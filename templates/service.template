[Unit]
Description=Service Identifier
After=network.target

[Service]
Type=simple
User={{NAME}}
WorkingDirectory=(getcwd())
ExecStartPre
ExecStart=
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
